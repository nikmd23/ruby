{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Hello",
            "type": "Ruby",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/hello.rb"
        }
    ]
}